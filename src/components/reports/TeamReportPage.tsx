import { TeamKpiPage } from '@/components/team/TeamKpiPage'

/**
 * Отдельная точка входа для отчета по команде (ТЗ 12.5).
 * Верстка переиспользует KPI-экран команды как базовый UI-шаблон.
 */
export default function TeamReportPage() {
  return <TeamKpiPage />
}
